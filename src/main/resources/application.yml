spring:
  application:
    name: DeepResearch
  ai:
    dashscope:
      api-key: ${AI_DASHSCOPE_API_KEY}
    mcp:
      client:
        enabled: true
        toolcallback:
          enabled: true
#        sse:
#          connections:
#            jina:
#              # "https://mcp.jina.ai"
#              url: "http://localhost:8787/v1/"
        request-timeout: 60000

server:
  port: 8080
